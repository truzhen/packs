# 询盘管理 Pack v0 派活卡

日期：2026-09-02　生命周期：`设计中`（骨架声明，不宣称已实现 / 已接线 / 已验收）

| 维度 | 本轮裁定 |
| --- | --- |
| 版本 / 优先级 | Truzhen v4 发布前 G4 W-C 窗口，零售询盘线（Domain Work Pack 骨架）。 |
| 我要做的事 | 把零售 CRM 七段链的段 1-4（线索 / 线索池 / 商机分诊 / 报价单）落成一个可复核的文件夹包声明：manifest 主权六件事、flow、角色槽 / 角色包、能力引用、lifecycle 胶水与离线契约测试。 |
| 真实客户 / 场景证据 | 依据 `docs/plans/sales-crm-private-domain-first-cashflow-minimum-loop-20260710.md`（该计划自标「缺证据」）+ Owner 2026-09-02 零售客户口径裁定。**本 Pack 仍缺真实客户原话与真实询盘记录**，不得据此升级成熟度。 |
| 最小交付 | Pack 声明 + flow + 角色 + 能力 + install / uninstall + 本 Pack 离线契约测试 + 根 README / MODULES 登记。不含真实 Provider、模型调用、前端组件与 E2E。 |
| 真相源 | 外部 ERPNext 持 `Lead` / `Quotation` / `Customer`；基座 05 只持受控快照，03 持回执，10 持发送候选与受控发送；本 Pack 只持声明与候选编排。 |
| 仓库 / 层归属 | 仅 `truzhen-packs/inquiry-management-pack-v0/**` 与根 `README.md` / `MODULES.md` / `pack_forbidden_artifacts_test.go` 的追加登记；不改其它 Pack、不改 `AGENTS.md`（§10 状态表行等协调线通知后再加）。 |
| 风险颜色 | 黄：manifest / flow / 角色 / 能力声明与离线测试。红：报价单写入、对外发送、Owner / Base Gate、Receipt——本轮只声明，不施工、不执行。 |
| 契约影响 | 不改 contracts、不改 OS 路由、不改 Provider 协议。新增两个声明层能力别名（`truzhen.capability.inquiry_triage`、`truzhen.external_snapshot.read`）与六个 04 契约操作名，属 Pack 侧声明，需 04 能力中心侧确认可解析。 |
| AI 角色 | 询盘经理是 Proposer：只出分诊、跟进草稿要点与报价要素候选；`delegation_allowed = false`，不得替 Owner 定价、承诺或发送。 |
| 禁止边界 | 不写真实客户名 / 手机号 / 金额；不自建客户主数据；不做通用 CRM；不做合同 / 回款 / 开票；不做群发额度与催办话术；不直连 ERP / 模型 / IM；不 push / merge / tag / deploy。 |
| 验收 | JSON 合法性、install / uninstall 语法、AGENTS §9 结构审计、`build_pack_bundle.py` 校验入口、本 Pack 契约测试、`GOWORK=off go test ./...`、禁品扫描、`git diff --check`。真实动作另需 Owner Gate 与可反查 Receipt。 |

## 已裁与已收敛

1. **Owner 第 15 条已裁（2026-09-02）**：报价单发给业主、业主付费。`quotation_to` = 服务主体，写死无可配置项（业主为线索 → `lead`；业主已成客户 → `customer`），渠道客户不得作 `quotation_to` / `party_name`。manifest / flow / README 三处已统一，可配置策略已删除。
2. **`template_family` 已按既有族收敛**：取 `长周期项目交付型`（truzhenos `softwareproject/template_family.go` 的 12 族固定目录内，`template_family_matrix_test.go` 钉死 `len == 12`）。目录中无「客户获取 / 销售」类；建议新增族「线索到报价客户获取型」需 14 制作台矩阵扩展，属跨仓治理动作，本窗口不做——见 README 待裁项 #2。未取「交易合同账款履约型」，因其含「收款对账」与本 Pack 排除的回款 / 开票边界冲突。
3. **flow 节点词表已按既有词表收敛，无需裁定**：全部 10 个 type 均在 14 制作台固定注册表（`packstudio/nodeinfo/nodeinfo.go`，17 词）内，已删除此前自造的 `gateway.communication_send`。该表无发送类节点，故跟进链路为 `followup_draft → owner_gate_send → followup_receipt`，真实发送由 10 ControlledExternalSend 在 Owner 确认后执行并落 03 回执（说明写在 `owner_gate_send.params`）。
4. **角色建议节点用 `AdviceCandidate`**：contracts 实有类型（`candidates/advice.go`，带 `cited_knowledge_refs`），属基座 06 `collaboration.advice` 节点语义，与 packs AGENTS §4.2「Pack 可生成的八类候选」不同层；测试锁成唯一例外且只允许挂 `collaboration.*` 节点。

## 待裁项

1. `AGENTS.md §10 当前包状态`表未加本 Pack 行（本任务禁止改 `AGENTS.md`），待协调线通知后补登记。
2. `pack_forbidden_artifacts_test.go` 的 `formalUninstallers` 已登记本 Pack 为正式卸载型（因裁定要求走 `/v3/pack-studio/lifecycle/uninstall`），与 smart-home 的停用型卸载语义不同，需 Owner 知悉。
