# G23 派活卡：墅学家 457 节点装修 Pack 商品化与长周期回放

日期：2026-07-24
生命周期：`已接线 -> blocked closeout`（本卡不是验收或发布声明）
版本 / 优先级：Truzhen v4 发布前当前主线，G23。

## 证据与最小交付

- 场景证据：Owner 指派的“墅学家 457 节点装修 Pack 商品化与长周期回放”；EPC 原版拓扑来源、457 节点 / 543 边保存在 `docs/epc-reference-topology.md`；已有去敏隔离回放记录在 `docs/测试报告.md`。
- 客户原话或生产使用记录：**缺证据**。本轮不得据此扩张行业规则、合同口径或正式状态；仅复核已有声明与去敏测试链。
- 最小可交付：固定 SHA 的隔离基座上完成一次去敏代表项目的安装、启用、五阶段长链、失败/阻断、重启回放、停用/重装闭环；产出紧凑 evidence envelope、parity 与 goal result。
- 明确砍掉：真实合同变更、付款、供应商通知、正式验收、售后关闭、文件外发、云市场上架、真实 Provider 接入；这些均留在 Owner Gate / backlog。

## 真相源、归属与边界

| 项目 | 归属与处理 |
| --- | --- |
| Pack 流程投影、角色、知识、能力引用 | `truzhen-packs/shuxuejia-renovation-pack-v0`；本轮可修改与验证。 |
| Transaction、Gate、Receipt、生命周期、项目 ReadModel | 固定 SHA `truzhenos` 测试实例；只通过 `127.0.0.1:18223` 验证，不修改 OS。 |
| 合同、付款、正式验收、售后关闭、供应商通知 | OS / 业务系统权威链；本轮只验证 candidate、Gate deny 或 blocked，零真实副作用。 |
| Provider 与外部软件 | OS Gateway / Provider 仓；不连接、不实现；缺失必须诚实 `provider_missing`、`not_ready` 或 `blocked`。 |

禁止边界：不改公共工具、其他 Pack、contracts、OS、Provider；不提交项目/合同/付款/供应商原文、DB、日志、secret 或大 artifact；不连接或部署 OpenShip；不 push、merge、tag、deploy。

## 风险与契约

- 黄：manifest、457 节点 flow、lifecycle 脚本与长链回放。只改本 Pack，改后执行全仓静态门和本 Pack 回归。
- 红：Base Gate、Receipt、权限、付款、发送、正式验收与售后关闭。不得施工或伪造；只验证 Owner/Base Gate 拒绝、候选或零副作用。
- 契约影响：默认只改 Pack 声明/测试证据，不改 schema、DTO、跨仓边界或主权链。若发现 OS/公共根因，登记 I04，不在本目标修复。

## 验收设计

1. 机器核验 457 个唯一节点、543 条边、八段阶段覆盖、边/跳转可达、协作角色槽、证据/Gate 要求及版本来源。
2. 在隔离实例使用去敏项目，覆盖设计准备、深化设计、材料/合同/付款、现场验收、售后；并覆盖变更、延期、材料替代、验收失败、售后重开。
3. 覆盖 restart、Receipt replay、provider missing、Memory scope、model transaction、communication blocked、payment candidate 零扣款、artifact export 受控、Gate deny 与 Receipt fail。
4. 复核 install/enable、disable/rollback、uninstall/reinstall，且不删除历史 Receipt 或正式知识。
5. 验收主体：本目标的独立静态/运行验收脚本与 Owner 对红色动作的最终复核；任何真实动作都必须先停在 Owner Gate。

完成后用户验收：检查 `docs/G23-closeout/goal-result.json` 和 `evidence-envelope.json` 中固定 SHA、端口、457 parity、代表场景、命令、Gate/Receipt 与清理/回滚；其中不得出现真实付款、合同、通知或外发成功。

## 2026-07-24 运行结论

旧 `7b115a` 的 uninstalled 重装语义缺口已由 R23C 修复并由当前权威 SHA `9d38cc` 复验取代。复验使用服务端签发的 Owner-presence 测试握手、空任务 software registry，并显式关闭 execution sidecar 与 Android 自动运行；没有伪造 Origin、cookie、Owner decision 或 Receipt。install、缺失/伪造决议拒绝、disable、reactivate、外部 proof 驱动的 formal uninstall 与 restart/replay 通过。当前阻断是同版本重装的第一个知识域 enable：FormalReceipt 幂等键与既有 payload 冲突，返回 `422/local_pack_install_failed` 并补偿回 uninstalled。本轮已关闭 devserver；详见 `docs/G23-closeout/`。
