# G19 派活卡：环保执法证据链 Pack 商品化与法律时点防线

## 裁定与最小交付

- **版本 / 优先级**：Truzhen v4 发布前目标 G19；生命周期从 `已接线` 向 `已验收` 验证，绝不据此宣称 `已发布`。
- **真实客户 / 场景证据**：缺证据。本轮只有 Owner 的发布前目标与既有 C01 去敏合成案情；没有可提交的真实当事人、真实案件或生产执法授权。最小交付因此限定为去敏、结构真实的验证场景与 Pack 声明/测试，真实执法链保持 `blocked`。
- **要做的事**：为环保执法候选链增加法律 `as_of` 防线、可反查来源/生效期口径、证据链字段口径和 G19 专项验收；复核安装、重启、停用、重装的隔离链路。
- **明确砍掉**：真实 OCR、真实模型文书正文、真实送达、处罚、查封、移送、整改复查，以及任何真实案件或个人信息；这些均不属于本轮可执行范围。

## 主权、归属与风险

- **真相源**：案件、Gate、Transaction、Receipt、Provider readiness 与正式知识归 `truzhenos`；本仓只拥有 Pack 声明、去敏测试输入与验收记录。法规事实以中国人大网 / 司法部 / 生态环境部等官方原文为准，Pack 索引始终是 `pending_human_review`。
- **仓库 / 层归属**：仅 `/Users/li/Documents/truzhen-packs/environmental-enforcement-pack-v0/`；不改 contracts、OS、Provider、客户端或公共工具。
- **风险颜色**：黄（Pack manifest/flow/测试）；橙（法律时点和运行端点的兼容性复核）；红（Base Gate、Receipt、真实执行/送达）仅做 fail-closed 验证，不施工、不绕过。
- **契约影响**：本轮只增强 Pack 内声明和校验，不新增或变更跨仓 schema、Gateway 或主权链；若 OS 不消费 Pack 的法律时点声明，登记为 I04，不能假称运行时已接线。
- **禁止边界**：不写真实案件、PII、OCR 产物、模型正文、DB、secret、日志；不改公共工具或其它 Pack；不 push/merge/tag/deploy。

## 验收与影响

- **去敏场景**：合成污水厂排放异常线索；只保留案发时点、证据类别、固定 SHA-256 样值和候选状态，不含个人、单位或可复原材料。
- **关键验证**：2026-08-15 前/后择法、来源与生效期、证据 source/hash、OCR `manual_handoff`、模型 `blocked`、非模板草稿要求、Gate deny、Receipt failure、重启，以及执行/送达/整改复查 Receipt 不可互代。
- **变更影响**：Pack flow/manifest/README、Pack 内去敏 fixture、专项测试与 closeout；不影响正式数据、权限、部署或任何外部动作。
- **独立验收主体**：Pack 静态测试与隔离 OS devserver 证据分别复核；Owner 可用本目录的 G19 closeout 中命令和返回状态复验。没有 Owner Gate/真实外部 Provider 时，相关项必须记录为 `blocked`，不将其计为通过。
