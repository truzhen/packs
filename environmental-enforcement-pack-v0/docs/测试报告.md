# 环保执法 Pack 验证报告（2026-06-26）

隔离 devserver（127.0.0.1:18099，含本分支 server.go + reactivate 端点改动，独立 state 库）真跑验证。

## 1. 与基座分离（核心）
- 全新产品进程启动后 `GET /v3/pack-studio/lifecycle/packs` → **registry total = 0**：基座不再自动焊装环保/智能家居 pack（`server.go defaultPlatformPackAssetSeeds()` 返回 nil）。
- 只有跑 `install.py` 后 pack 才出现，跑 `uninstall.py` 后消失。**pack 与基座主程序解耦成立。**

## 2. 加载（install.py）
- 走真实 lifecycle 端点：canvas→06 同步 → draft（六件事+15 知识域）→ readiness → promote → confirm（真 01 Base Gate + 03 receipt）。
- 2 角色包（执法精英/挑剔律师）经角色 lifecycle enabled；槽位绑定 enabled。
- 知识库：**752 条 FormalKnowledge** 经 `/v3/memory/knowledge/batches` + Base 签发 approve decision + approve 入库，均 `pending_human_review`。
- 独立 API 复核：`lifecycle/packs` enabled_version=1.0.0；`memory/knowledge/formal` 真检索到知识。

## 3. 卸载（uninstall.py）
- 经真实 `lifecycle/disable`（Base 签发 disable 决议）→ enabled_version 清空、知识域级联卸载。
- 历史回执保留可反查（卸载≠删历史）。

## 4. 隔离
- 卸载环保后，智能家居 pack 仍 enabled（互不影响）。

## 5. 重载（unload 后再 load）——同版本 reactivate（后端新增能力）
- 后端新增 `/v3/pack-studio/lifecycle/reactivate`：把 disabled/rolled_back 版本经真 Base gate + 03 receipt 单跳 `disabled→enabled`，并重挂知识域（`driveEnableMounts`）。decision 由 Base 签发（与 confirm 同纪律，非调用方自带）。
- install.py 重载逻辑：未 enabled 时**优先同版本 reactivate**；记录从未存在才走完整 draft 路径。
- 实测（反复两轮）：install(@1.0.0,752 知识) → uninstall(disabled,知识0) → install **reactivate 回 @1.0.0**（不 bump 版本）、知识重挂回 100（页上限，实 752）→ 再 uninstall→reload 仍 @1.0.0/知识在。**unload 后可反复 reload，版本不漂移、知识不重复。**
- Go 回归测试 `TestLifecycleReactivateReenablesDisabledVersion` 通过（enable→disable→reactivate 同版本→幂等重放）。

## 6. 现状
- Go 改动：server.go（产品自动 seed 摘除 + 测试 fixture 拆分）+ lifecycle `Reactivate` 方法（reactivate_flow.go）+ stage4 `handleLifecycleReactivate` 端点。
- build 通过；ProductSeed/Lifecycle/Disable + 新增 Reactivate 测试全绿。
- 未 commit 时曾遇工具静默写失败（已用 python read-back 核验绕开）。

> 注：知识内容来自 Owner 提供的权威资料（生态环境法典/执法指南/案例精析）真实导入，pending_human_review，正式适用前须经法务核验。

---

# 本轮增补：2026-07-02 / 07-03 真实实测结果

本轮由一位区县环保执法大队中队长以真实一线身份实测（用户日志 p3/p4-user-log），并由本次「批 7 内容完善」补跑隔离 devserver E2E。以下为事实口径，只记已核实结果与已知阻断，不夸大。

## 8. 主权链回执真通（03 反查铁证）
- 隔离 devserver（127.0.0.1:18099，binary 从 truzhenos main c8c3702 现编，HOME 指向临时目录、状态库全隔离，不污染正式库）。
- install.py 走真实 lifecycle：canvas→06 同步（engine_sync.synced=true）→ draft（六件事 + 15 知识域）→ readiness → promote → confirm（真 01 Base Gate + 03 receipt）。
- reactivate 路径实测产 Base 签发 decision_ref（如 decision_96657f85…）+ receipt_ref `718b0baa-9415-4765-8dc5-71346f56565e`，经 `GET /v3/receipts/{ref}` **03 反查 HTTP 200**，回执含 receipt_ref/candidate_ref/transaction_ref/decision_ref/status/actor/event_type——回执可反查原文成立。
- 用户实测轮（p3/p4）同链路曾产回执 9144865e（03 反查）；卸载后历史回执仍可反查（卸载≠删历史）。

## 9. 9 阶段推进实测（run 至 lawyer_challenge）
- p4「鑫源电镀厂夜间偷排废水执法案」经事务对象工作台「新建并起 run」真起 run（run-environmental-enforcement-flow-…），跑通 线索受理→初筛立案→现场检查准备→出示证件/告知→现场检查→材料扫描→证据链归集→证据三性审查→违法事实候选，run 推进至 lawyer_challenge（律师质询）。
- 真解锁 screening 的是 PDF 解析（路由条件 pdf_parse_status == parsed）：现场举报记录 PDF 经真解析（DocumentParseCandidate·candidate_ready·1 页·291 字，字数与文件吻合），产回执候选号并挂本案 tx，四条边界声明齐全（candidate_only/no_formal_write/no_business_object_update/no_formal_task_create）。
- 秘书长对话真模型真流式（Qwen3.6-35B-A3B-4bit·local omlx·live），线索受理/证据三性/处置推演/文书草稿多轮回复对题专业，每轮产 candidate_only 候选卡带三凭证。

## 10. 双角色 model_not_ready 诚实阻断
- fact_advice 节点显式声明「角色槽位 enforcement_elite 已绑定，但模型网关未真实产出（provider_fallback），受控阻断（model_not_ready）：不伪造角色文本」——fail-closed 正确，未编造角色发言。
- run 视图双角色位（执法精英 enforcement_elite / 挑剔律师 critical_lawyer）真实存在并标 candidate_only/non_formal；本轮角色 advice/challenge 未真实产出即受控阻断，未假成功。

## 11. 送达 provider_missing 受控拒绝
- 要求「正式送达鑫源电镀厂负责人」并点「让秘书动手执行」：秘书动作流面板返回 **provider_missing**——无假成功、无真送达，受控拒绝真发生。
- 与 manifest 声明一致：outbound_legal_document_delivery（doc_delivery，gateway communication，risk high）provider 未接入时受控降级；本轮完善已把该门槛口径对齐进 manifest.provider_requirements（含 description）。

## 12. 本轮内容完善验证（批 7）
- 流程 stage_guide：19 节点全补一线执法口径，现场检查/调查询问采用队长手写原文；隔离 E2E 复核 canvas→06 投影内含 stage_guide 及「时间水印/签字捺印/排污许可证」原文——新提示语随 pack 真进入引擎。
- manifest 门槛对齐：monitoring_data_fetch 补 optional:true（与 capabilities.json/install.py 转发口径一致）；5 项 provider_requirement 补 description；模型硬门槛 legal_doc_draft（gateway model / fallback blocked）与 required_capabilities 保持一致，未改安装行为。
- 知识域核对：manifest.knowledge_scopes(15) = knowledge-scopes.json(15) = knowledge/ 子目录(15) = knowledge-index 引用 scope(15)，45 条 index 条目全指向实存文件，零漂移；无 solid-waste 残留（上轮已删的假声明未回潮）。
- 隔离 E2E 结果：registry 0 → 1（install，enabled 1.0.0，752 条 FormalKnowledge）→ 1 → 0（uninstall，enabled 清空、知识域级联卸载、历史回执保留），进程杀净、临时目录清理，正式 18080 未受影响。

> 本轮所有正式动作仍经 Owner + Base Gate 裁定并产 Receipt；角色与模型均为 Proposer，未接通能力一律 blocked / provider_missing / model_not_ready 受控降级，不假成功。
