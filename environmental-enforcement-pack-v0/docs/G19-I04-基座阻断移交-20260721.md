# G19 → I04 基座阻断移交：知识域启用的 Owner Gate 消费链

状态：`blocked`（2026-07-21）。本文件只记录跨仓根因，不请求 Pack 绕过或模拟 Owner 确认。

## 可复核事实

- 基座仓只读测试目标：`/Users/li/Documents/truzhenos`，固定 SHA `29e2b0e78addcdf2f5edf48b52f440d8748c2743`。
- 隔离实例：`http://127.0.0.1:18219`，全部状态写入一次性 `TRUZHEN_DATA_DIR`，已在测试后移入废纸篓；未触及生产状态。
- 环保 Pack 的 canvas 写穿成功，读回包含 `pack_policy://environmental-enforcement/legal-as-of-v1` 和 `DocumentDraft`。
- `install.py` 依次完成 canvas、draft、readiness、promote；在 lifecycle `confirm` 收到 HTTP 422：
  `pack_knowledge_mount_enable_explicit_owner_confirmation_consumer_not_wired`。
- 该拒绝发生在必需知识域 `knowledge_scope://environmental/air` 挂载；因此没有 enabled version，也没有合法条件继续角色绑定、知识入库、去敏案件跑线、重启或 disable/reinstall 验证。

## 所需基座修复（I04 归属）

为 Pack lifecycle 的知识挂载计划提供可信 GUI / Owner presence 的显式确认消费者，并让确认后的结果经 Base Gate 和 Receipt 链回到 lifecycle confirm。要求 fail-closed：未确认、确认凭据缺失、Receipt 失败或重启后状态不一致时仍为 `blocked`，不得为 Pack 脚本增加 POST 旁路或伪造 owner evidence。

## 回归验收

1. 未确认挂载计划：confirm 保持 422/blocked，且无 enabled version。
2. Owner 在可信前台确认且 Base Gate 通过：required knowledge scopes 全部挂载，enabled version 可读回，并有可反查 Receipt。
3. 重启后上述 enabled/mount/Receipt 关系保持；disable/rollback/uninstall/reinstall 不删除历史 Receipt。
4. G19 在同一去敏场景复跑后，再验证 as_of 前后、OCR `manual_handoff`、模型 `blocked`、Gate deny、Receipt failure 与三类完成 Receipt 不可互代。

本仓不改基座、contracts、Provider 或公共工具；修复完成后需 Owner 重新授权跨仓接线验收。
